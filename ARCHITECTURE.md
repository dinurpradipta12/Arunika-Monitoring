# Arunika Integration Architecture

Visualisasi lengkap sistem integrasi Arunika Monitoring.

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     EXTERNAL APPLICATIONS                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ Web App 1    │  │ Web App 2    │  │ Mobile App / Other   │   │
│  │ (React/Vue)  │  │ (Node.js)    │  │ (Python/PHP/etc)     │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────────────┘   │
│         │                 │                  │                   │
│         │ Register User   │ Register User    │ Register User     │
│         │ Log Activity    │ Log Activity     │ Log Activity      │
└─────────┼─────────────────┼──────────────────┼───────────────────┘
          │                 │                  │
          │ HTTP POST       │ HTTP POST        │ HTTP POST
          │ JSON Payload    │ JSON Payload     │ JSON Payload
          │ + API Key       │ + API Key        │ + API Key
          │                 │                  │
          ▼                 ▼                  ▼
     ┌────────────────────────────────────────────┐
     │   ARUNIKA MONITORING API (Backend)          │
     │   ┌──────────────────────────────────────┐  │
     │   │ Webhook Handler                      │  │
     │   │ - Validate API Key                   │  │
     │   │ - Verify signature                   │  │
     │   │ - Validate timestamp                 │  │
     │   └──────────┬───────────────────────────┘  │
     │              │                              │
     │   ┌──────────▼───────────────────────────┐  │
     │   │ Event Processor                      │  │
     │   │ - Parse event type                   │  │
     │   │ - Route to handler                   │  │
     │   │ - Process data                       │  │
     │   └──────────┬───────────────────────────┘  │
     │              │                              │
     │   ┌──────────▼───────────────────────────┐  │
     │   │ Database Layer                       │  │
     │   │ - Save user                          │  │
     │   │ - Save activity                      │  │
     │   │ - Update stats                       │  │
     │   └──────────┬───────────────────────────┘  │
     │              │                              │
     │   ┌──────────▼───────────────────────────┐  │
     │   │ Real-Time Event Broadcaster          │  │
     │   │ - Emit via WebSocket                 │  │
     │   │ - Or prepare for polling             │  │
     │   └──────────┬───────────────────────────┘  │
     └─────────────┼──────────────────────────────┘
                   │
                   │ Real-time Event
                   │ WebSocket/Polling
                   │
     ┌─────────────▼──────────────────────────────┐
     │   ARUNIKA DASHBOARD (Frontend)              │
     │   ┌──────────────────────────────────────┐  │
     │   │ User Management                      │  │
     │   │ - Display new users (instant)        │  │
     │   │ - Approve pending users              │  │
     │   │ - Manage user status                 │  │
     │   └──────────────────────────────────────┘  │
     │   ┌──────────────────────────────────────┐  │
     │   │ App Management                       │  │
     │   │ - Manage connected apps              │  │
     │   │ - Generate API keys                  │  │
     │   │ - Test webhooks                      │  │
     │   └──────────────────────────────────────┘  │
     │   ┌──────────────────────────────────────┐  │
     │   │ Activity Monitoring                  │  │
     │   │ - View activity logs                 │  │
     │   │ - Filter by user/app/type            │  │
     │   │ - Search activities                  │  │
     │   └──────────────────────────────────────┘  │
     │   ┌──────────────────────────────────────┐  │
     │   │ Analytics & Reporting                │  │
     │   │ - User growth charts                 │  │
     │   │ - Activity statistics                │  │
     │   │ - Revenue tracking                   │  │
     │   └──────────────────────────────────────┘  │
     └──────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

### User Registration Flow

```
┌────────────────────────────────────────────────────────┐
│ User Clicks "Sign Up" in Your App                      │
└────────────────┬─────────────────────────────────────┘
                 │
     ┌───────────▼──────────────┐
     │ Submit Registration Form │
     │ (email, name, password)  │
     └───────────┬──────────────┘
                 │
     ┌───────────▼──────────────────────────────┐
     │ Your App Processes Registration          │
     │ - Validate input                         │
     │ - Hash password                          │
     │ - Create user in DB                      │
     └───────────┬──────────────────────────────┘
                 │
     ┌───────────▼──────────────────────────────┐
     │ Call arunika.registerUser()              │
     │ {                                        │
     │   userId: "user-123",                   │
     │   email: "user@example.com",            │
     │   name: "John Doe",                     │
     │   subscriptionTier: "free"              │
     │ }                                        │
     └───────────┬──────────────────────────────┘
                 │
     ┌───────────▼──────────────────────────────┐
     │ SDK Sends HTTP POST Request              │
     │ - URL: /api/webhooks                     │
     │ - Headers: X-API-Key, Content-Type       │
     │ - Body: JSON with user data              │
     └───────────┬──────────────────────────────┘
                 │
     ┌───────────▼──────────────────────────────┐
     │ Arunika Validates & Processes            │
     │ - Verify API key                         │
     │ - Check timestamp                        │
     │ - Save to database                       │
     │ - Emit real-time event                   │
     └───────────┬──────────────────────────────┘
                 │
     ┌───────────▼──────────────────────────────┐
     │ Dashboard Receives Real-Time Update      │
     │ - User appears instantly                 │
     │ - Status: "pending" (needs approval)    │
     │ - Activity log updated                   │
     └────────────────────────────────────────┘
```

### Activity Logging Flow

```
┌──────────────────────────────────┐
│ User Does Something in Your App  │
│ (Login, Page View, Purchase)     │
└────────────┬─────────────────────┘
             │
┌────────────▼─────────────────────┐
│ Trigger Activity Logger           │
│ arunika.logActivity(              │
│   userId,                         │
│   'login',                        │
│   'User logged in'                │
│ )                                 │
└────────────┬─────────────────────┘
             │
┌────────────▼─────────────────────────────┐
│ SDK Queues Activity Event                 │
│ (Auto batches multiple events)           │
└────────────┬─────────────────────────────┘
             │
┌────────────▼─────────────────────────────┐
│ Batch Processor (every 5s or 10 events)   │
│ - Collect queued events                   │
│ - Send to webhook                         │
└────────────┬─────────────────────────────┘
             │
┌────────────▼─────────────────────────────┐
│ Arunika Processes Activity                │
│ - Store in activity log                   │
│ - Update user last_active                 │
│ - Broadcast to dashboard                  │
└────────────┬─────────────────────────────┘
             │
┌────────────▼─────────────────────────────┐
│ Dashboard Shows Activity                  │
│ - User activity timeline updated          │
│ - Stats recalculated                      │
│ - Heatmaps updated                        │
└──────────────────────────────────────────┘
```

---

## 🔌 Integration Methods

```
┌─────────────────────────────────┐
│   Your Application              │
└────────────┬────────────────────┘
             │
      ┌──────┴──────┐
      │             │
      ▼             ▼
┌──────────┐  ┌──────────────┐
│ Method A │  │  Method B    │
│   SDK    │  │ Direct HTTP  │
└──────────┘  └──────────────┘
      │             │
      │ Recommended │ For
      │ for:        │ any
      │ - React     │ platform
      │ - Vue       │
      │ - Node.js   │
      │ - TypeScript│
      │             │
      ▼             ▼
   ┌──────────────────────┐
   │ Arunika Webhook API  │
   └──────────────────────┘
```

**Method A: SDK (Recommended)**
- Pre-built functions
- Auto-batching
- Queue management
- Error handling

**Method B: Direct HTTP**
- Raw API calls
- Full control
- Works with any language
- Need to handle batching yourself

---

## 🔐 Security Flow

```
┌────────────────────────────────────────┐
│ Your App Sends Webhook Request         │
└────────────┬─────────────────────────┘
             │
┌────────────▼──────────────────────┐
│ Headers Included:                  │
│ - X-API-Key: sk_live_xxx...       │
│ - Content-Type: application/json   │
│ - X-Webhook-Signature: hmac-sha256 │
│ - X-App-ID: app-xxx               │
└────────────┬──────────────────────┘
             │
┌────────────▼──────────────────────┐
│ Arunika Validates:                 │
│ ☐ API Key format correct?         │
│ ☐ API Key registered in DB?       │
│ ☐ Signature matches payload?       │
│ ☐ Timestamp not too old?           │
│ ☐ App ID matches API Key?          │
└────────────┬──────────────────────┘
             │
      ┌──────┴──────┐
      │             │
      ▼             ▼
  ✅ Valid     ❌ Invalid
      │             │
      │             └─► Log as suspicious
      │                 Return 401 error
      │
      ▼
Process Event
```

---

## 📈 Real-Time Update Mechanism

### Current: Polling (Immediate deployment)
```
Dashboard              Arunika API
    │                     │
    │──────────────────────> GET /users?appId=app-123
    │                     │
    │ Every 5 seconds      │
    │                      Get latest users
    │                      │
    │<──────────────────── User list with changes
    │                      │
    ├─ Update UI           │
    ├─ Show new users      │
    ├─ Update stats        │
    │                      │
    │──────────────────────> GET /activities?appId=app-123
    │                      │
    │                      Get latest activities
    │                      │
    │<──────────────────── Activity log with new entries
```

### Future: WebSocket (Real-time, 0 latency)
```
Dashboard                Arunika WS Server
    │                           │
    │──── ws.connect ─────────> │
    │<──── connected ────────── │
    │                           │
    │                    User registered
    │<──── {event: user_created, ...} ─
    │                           │
    │ Instant update           │
    │ No delay!                │
    │                           │
    │                    Activity logged
    │<──── {event: user_activity, ...} ─
    │                           │
    │ Instant update           │
```

---

## 💾 Database Schema (Conceptual)

```
┌─────────────────┐
│ apps            │
├─────────────────┤
│ id              │
│ name            │
│ description     │
│ api_key         │
│ created_at      │
│ status          │
└────────┬────────┘
         │ has many
         │
┌────────▼─────────────────┐
│ users                    │
├──────────────────────────┤
│ id                       │
│ app_id (FK)              │
│ external_user_id         │
│ email                    │
│ name                     │
│ subscription_tier        │
│ status                   │
│ registered_at            │
│ last_active              │
└────────┬─────────────────┘
         │ has many
         │
┌────────▼──────────────────┐
│ activities                 │
├────────────────────────────┤
│ id                         │
│ user_id (FK)              │
│ app_id (FK)               │
│ activity_type             │
│ description               │
│ metadata                  │
│ timestamp                 │
└────────────────────────────┘

Indexes:
- users: (app_id, id)
- activities: (user_id, timestamp DESC)
- activities: (app_id, timestamp DESC)
```

---

## 🔄 Batch Processing (SDK Internal)

```
Application Timeline:

T=0s: arunika.logActivity(user1, 'login')
       Event 1 → Queue [Event 1]

T=1s: arunika.logActivity(user2, 'page_view')
       Event 2 → Queue [Event 1, Event 2]

T=2s: arunika.logActivity(user3, 'purchase')
       Event 3 → Queue [Event 1, Event 2, Event 3]

...

T=5s: Timer triggers batch send
      Send [Event 1, Event 2, Event 3, ..., Event 8]
      Queue cleared

T=5.1s: Response from Arunika
        ✓ All 8 events processed
        Queue empty again

T=6s: arunika.logActivity(user4, 'logout')
       Event 9 → Queue [Event 9]

...

T=10s: Timer triggers batch send again
       Send [Event 9, Event 10, ...]
```

**Benefits:**
- Fewer HTTP requests (8 events = 1 request instead of 8)
- Better performance
- Reduced network load
- Automatic retry if offline

---

## 📱 Multi-Platform Support

```
┌──────────────────────────────────────────────────────────┐
│                    Arunika Backend                        │
└──────────────────────────────────────────────────────────┘
                      │
         ┌────────────┼────────────┬──────────────┬────────┐
         │            │            │              │        │
         ▼            ▼            ▼              ▼        ▼
┌────────────┐ ┌──────────┐ ┌────────────┐ ┌──────────┐ ┌────────┐
│ React App  │ │ Vue.js   │ │ Next.js    │ │ Node.js  │ │ Python │
│ (SDK)      │ │ (SDK)    │ │ (SDK+API)  │ │ (SDK)    │ │ (HTTP) │
└────────────┘ └──────────┘ └────────────┘ └──────────┘ └────────┘

         ▼            ▼            ▼              ▼        ▼
         └────────────┼────────────┴──────────────┴────────┘
                      │
                HTTP Webhooks
                      │
         ┌────────────▼────────────┐
         │  Unified API Endpoint   │
         │  /api/webhooks          │
         └────────────┬────────────┘
                      │
                 Process Event
                      │
         ┌────────────▼────────────┐
         │   Store in Database     │
         └────────────┬────────────┘
                      │
         ┌────────────▼────────────┐
         │  Broadcast to Dashboard │
         └────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
Your App Server           Arunika Monitoring           Your Users
      │                         │                         │
      │                         │                         │
  ┌───┴──────┐          ┌──────┴──────┐           ┌───────┴────┐
  │ Frontend  │          │ API Server  │           │ Browser    │
  │ (React)   │─────────→│ (Node.js)   │←─────────│ (Display)  │
  └───┬──────┘          │             │           └────────────┘
      │                  │ - Webhook   │
      │                  │ - Process   │
      │                  │ - Broadcast │
      │                 └──────┬──────┘
      │                        │
      │                  ┌─────┴──────┐
      │                  │ Database    │
      │                  │ (MongoDB    │
      │                  │  or SQL)    │
      │                  └─────┬──────┘
      │                        │
      │              ┌─────────┴───────┐
      │              │                 │
  ┌───┴──────┐      │          ┌──────┴─────┐
  │Backend    │      │          │ WebSocket  │
  │(Node/Py) │──────┘          │ Server     │
  └──────────┘                  └────────────┘
     (Sends webhooks)           (Real-time updates)
```

---

## ✅ Complete Integration Checklist

```
Planning Phase:
  ☐ Define what events to track
  ☐ Plan database schema
  ☐ Document API endpoints
  ☐ Design webhook format

Development Phase:
  ☐ Setup backend server
  ☐ Create database tables
  ☐ Implement webhook handler
  ☐ Create API endpoints
  ☐ Implement event processor
  ☐ Create dashboard frontend

Integration Phase:
  ☐ Generate API keys
  ☐ Test with cURL
  ☐ Integrate SDK in test app
  ☐ Test user registration flow
  ☐ Test activity logging
  ☐ Verify real-time updates

Deployment Phase:
  ☐ Deploy backend
  ☐ Deploy frontend
  ☐ Configure webhooks
  ☐ Setup monitoring
  ☐ Setup backups
  ☐ Launch documentation
  ☐ Announce integration

Post-Launch:
  ☐ Monitor error rates
  ☐ Track performance metrics
  ☐ Gather user feedback
  ☐ Plan improvements
  ☐ Scale as needed
```

---

**Last Updated:** February 3, 2026
